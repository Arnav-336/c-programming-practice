#include<stdio.h>
int main()
{
    float length,breadth,area;
    length=3.5;
    breadth=7.5;
    area=length*breadth;
    printf("Area of a rectangle having sides %f and %f is %f",length,breadth,area);
}