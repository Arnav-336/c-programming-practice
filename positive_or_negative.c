#include<stdio.h>
int main()
{
    int no=0;
    if (no>0)
    printf("The number %d is a positive number",no);
    else if (no==0)
    printf("The number %d is neither positive nor negative",no);
    else
    printf("The number %d is a negative number",no);
}