#include<stdio.h>
#include<math.h>
int main()
{
    int no,root;
    printf("Enter the number to know the square root of: ");
    scanf("%d",&no);
    root=sqrt(no);
    printf("The square root of %d is %d",no,root);
}