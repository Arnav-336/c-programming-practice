#include<stdio.h>
int main()
{
    float base,height,area;
    base=8.5;
    height=6;
    area=(base*height)/2;
    printf("The area of a triangle have base %f and height %f is %f",base,height,area);
}